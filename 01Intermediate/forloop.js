const days = ['Mon','Tue','Wed','Thu','Fri','Sat']

//index++ can be writen as index = index + 1
// index <= length-1
for(let index = 0; index < days.length; index++){
    const element = days[index]
    console.log(element)
    //console.log(days[index])
}